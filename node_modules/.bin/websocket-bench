#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../websocket-bench/bin/websocket-bench" "$@"
  ret=$?
else 
  node  "$basedir/../websocket-bench/bin/websocket-bench" "$@"
  ret=$?
fi
exit $ret
